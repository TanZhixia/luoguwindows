#include <iostream>
#include <cstring>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, m;
    int a[100005], diff[100005];
    memset(diff, 0, sizeof(diff));
    cin >> n >> m;
    int last;
    cin >> last;
    m --;
    while (m --) {
        int to;
        cin >> to;
        
    }
}